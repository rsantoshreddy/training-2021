const firstName = 'x';
const lastName = 'y';

let value;

value = firstName + lastName;

vlaue += 'z';

// Escaping
value = "that's awsome";

//length
value.length;

// concat
value = firstName.concat(' ', lastName);

// cahnge case

value = firstName.toUpperCase();
value = firstName.toLowerCase();

value = firstName[0];
value = firstName[2];

// indexOf()
value = firstName.indexOf('x');
value = firstName.lastIndexOf('x');

// charAt();
value = firstName.charAt(3);

// substring

value = firstName.substring(0, 4);

// slice()
value = firstName.slice(0, 4);
value = firstName.slice(-3);

// split()

value = 'hi there'.split(' ');

// replace()
value = firstName.replace('x', 'y');

// includes()
value = firstName.includes('hi');

// string literal
